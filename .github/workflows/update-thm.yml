name: Update TryHackMe Stats

on:
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

jobs:
  update-stats:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests

      - name: Debug — vis filstruktur
        run: find . -name "*.py" | head -20

      - name: Fetch and update THM stats
        run: python portfolie/scripts/update_thm.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add portfolie/projekter.html
          git diff --staged --quiet || git commit -m "Auto: opdater TryHackMe stats"
          git push